"""Reminders — each is a small unit of "show something to the user".

Add a feature by writing a new module here (subclassing ``Reminder``) and
registering it with the ``ReminderScheduler`` in ``app.py``.
"""
